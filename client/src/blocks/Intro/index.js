import styled from 'styled-components';
import LandGrid from './LandGrid';

const Intro = styled.div`
  width: '100%',
  margin: 'auto' 
`;

Intro.LandGrid = LandGrid;

export default Intro;
